# speculative_decoding_test

A tiny experiment project for speculative decoding with Hugging Face models.

## Files / Folders

- `baseline.py`: main experiment script.
- `\results`: results generated.

## Quick start

1. Install dependencies:

```bash
pip install torch transformers bitsandbytes accelerate
```

2. Run the baseline script:

```bash
python baseline.py
```
